// Placeholder module kept for compatibility; admin client is now created inline in API routes.
// This avoids editor type-resolution issues with '@supabase/supabase-js' in some setups.
export const supabaseAdmin: unknown = null;
